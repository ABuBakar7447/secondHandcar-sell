import React from 'react';
//this one also not using anywhere
const AllSellerCard = ({user, index,handleUserDelete}) => {
    const {_id, user_name, role, email} = user
    return (
        <div></div>
    );
};

export default AllSellerCard;